---
title: ""
format:
  dashboard: 
    orientation: columns
---

## Column {width = "35%"}

Placeholder text for column 1

```{r, install_packages}
#| echo: false
#| include: false
library(tidyverse)
```

```{r, printstarwars}
x <- starwars
head(starwars)
```

```{r, sendtoojs}
#| echo: false
#| include: false
ojs_define(service_name = x)
```


```{ojs}
//| expandable: false

// Transpose data to make it useable in ojs
service = transpose(service_name)

// Create dropdown menu of the names
viewof serviceDropdown = Inputs.select(
  service.map(d => d.name),
  {
    label: "Choose a service",
    unique: true
    }
)
```

## Column {width = "65%"}

### Row {height = "10%"}

Placeholder text for column 2, row 1

### Row {height = "90%"}

```{ojs}
viewof search = Inputs.search(service, {placeholder: "Type to find a service"})

viewof items = Inputs.table(search, {
columns: ["name"],
header: {name: "Care Service"},
width: 300, 
maxHeight: 250,
required: false,
value: search, 
multiple: false})
```